import React from "react";

import FramedPainting from "../FramedPainting/FramedPainting";

function Gallery(props) {
    // set a variable equal to a new array
    // take all of the paintings with a showing value of true
    // add these paintings to the new array
    // for each of these paintings, MAP
        // Feed the props of the painting to the Framed Painting components
        // rememeber you will need two returns here, one outside the map and one inside

        <FramedPainting 
            // PROPS STUFF GOES HERE
        />
    // 
}

export default Gallery;